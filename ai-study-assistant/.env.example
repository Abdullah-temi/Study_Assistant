OPENAI_API_KEY=

# Matches the credentials in docker-compose.yml (service: db).
# Run `docker compose up -d db` before starting the backend or running Alembic.
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/ai_study_assistant

JWT_SECRET=change-me-before-production
FRONTEND_ORIGIN=http://localhost:5173

